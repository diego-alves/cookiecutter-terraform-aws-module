# Cookiecutter Terraform AWS Module

This repo has an cookiecutter(template) for an terraform aws module based on the Standard Module Structure proposed by HashiCorp, so it is compactible with the registry.
